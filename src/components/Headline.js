import styled from "styled-components";

export default styled.p`
  font-family: "Source Code Pro", monospace;
  font-size: 1.25rem;
  font-weight: 600;
  opacity: 0.5;

  a {
    font-style: normal;
    font-weight: normal;
  }
`;
